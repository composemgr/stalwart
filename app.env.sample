# Stalwart Mail Server - All-in-one mail server (SMTP/IMAP/JMAP)
# Copy to app.env and customize before starting

# Timezone
TZ=America/New_York

# Hostname (used as mail server FQDN, e.g. mail.example.com)
BASE_HOST_NAME=${HOSTNAME}
